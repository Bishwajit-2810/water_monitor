// valueNotifier : holds the date
// valueListenableBuilder : listen to the data (don't need setState)

import "package:flutter/material.dart";

ValueNotifier<bool> isDarkThemeNotifier = ValueNotifier(true);
